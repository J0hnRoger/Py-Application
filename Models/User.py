class User(object):
    """docstring for User"""
    def __new__(cls, name, firstName):
        instance = super(Temperature, cls).__new__(cls, value)
        instance.unit = unit
        return instance
